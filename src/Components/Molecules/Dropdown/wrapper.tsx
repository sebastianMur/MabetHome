import styled from 'styled-components';

export const Wrapper = styled.div`
  min-height: 15rem;
  min-width: 15rem;
  color: black;

  .items {
    li {
      font-size: 1.76rem /* 18px */;
      line-height: 2rem; /* 28px */
      font-weight: 400;
    }
    position: relative;
  }
`;
